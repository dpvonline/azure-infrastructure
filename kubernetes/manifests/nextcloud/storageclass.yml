apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: nextcloud-azuredisk
provisioner: disk.csi.azure.com
allowVolumeExpansion: true
volumeBindingMode: WaitForFirstConsumer
parameters:
  skuname: Standard_LRS
  kind: Managed
