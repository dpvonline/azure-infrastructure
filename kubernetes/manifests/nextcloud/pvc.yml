apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nextcloud-pvc
  namespace: nextcloud
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: azuredisk-standard-retain
  resources:
    requests:
      storage: 64Gi
